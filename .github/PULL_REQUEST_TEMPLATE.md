# Add wrapper to registry

## Description / motivation

<!-- What does your wrapper do? Why is it useful? Link to the npm package and the wrapper's repo. -->

## Checklist

- [ ] My wrapper is published to npm under a stable scope
- [ ] LICENSE present in package
- [ ] README documents action list with paramSchemas
- [ ] No `postinstall` script (security)
- [ ] Declared `transport` actually works (we'll smoke-test)
- [ ] `qontinui.wrapper` manifest field present in package.json
- [ ] Entry added to `registry.json` conforms to `schemas/wrapper-entry.schema.json`

## Notes for reviewers

<!-- Anything maintainers should know — e.g., this wrapper requires an OAuth flow, has a free tier, etc. -->
